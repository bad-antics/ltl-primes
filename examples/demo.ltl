// examples/demo.ltl — runnable demo for ltl_primes

import std::io
use ltl_primes as lib

fn main() {
    io::println("ltl_primes demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}